<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="480dp"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    app:cardBackgroundColor="#1A2232"
    app:cardCornerRadius="16dp"
    app:cardElevation="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Actualización Disponible"
            android:textColor="#FFD700"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- Subtitle/Details -->
        <TextView
            android:id="@+id/update_version_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Versión: 1.1 (Código 2)"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Changelog label -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Novedades:"
            android:textColor="#A0AABF"
            android:textSize="14sp"
            android:textStyle="bold" />

        <!-- Changelog container -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_marginTop="8dp"
            android:background="#12FFFFFF"
            android:padding="12dp">
            <TextView
                android:id="@+id/update_changelog"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="• Corrección de errores\n• Optimización de velocidad\n• Nuevo diseño de tarjetas"
                android:textColor="#E2E8F0"
                android:textSize="13sp"
                android:lineSpacingExtra="3dp" />
        </ScrollView>

        <!-- Action Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:orientation="horizontal"
            android:gravity="end">

            <!-- Cancel Button -->
            <Button
                android:id="@+id/btn_cancel_update"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="12dp"
                android:backgroundTint="#2D3748"
                android:text="Más Tarde"
                android:textColor="#FFFFFF"
                android:focusable="true"
                android:nextFocusRight="@+id/btn_confirm_update" />

            <!-- Confirm Button -->
            <Button
                android:id="@+id/btn_confirm_update"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:backgroundTint="#FFD700"
                android:text="Actualizar"
                android:textColor="#0E121A"
                android:focusable="true"
                android:nextFocusLeft="@id/btn_cancel_update" />

        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>
